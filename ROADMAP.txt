# Features / evolution:

## Training list

* add support for contextual workout
** prefilter results on it
** pre-select it with new create modal

* add filter on views:
** per routine
** per workout
** by date ranges

* order columns asc/desc

* add summary for total result of the search, like:
** number of sets
** reps
** weights

* add graphic (I need to be fired to have some times for this ( :p :/ )

## Others

* add a refresh link, which redo the $vm.mount() or something like that
* list of exercises grouped by muscles, and ordered alpha
* autocomplete on list of model instances like exercises
* add a note on workout exercise set
* dat frmat in front (training list at least)