{% extends 'base.html' %}
{% load i18n static %}

{% block title %}{% trans 'Exercises' %}{% endblock %}
{% block content %}
<div class="card mb-3">
  <div class="card-header">
    <i class="fas fa-table"></i>
    {% trans 'Exercises' %}
  </div>
  <div class="card-body">
    <div class="table-responsive">

      <table class="table table-bordered" width="100%" cellspacing="0">
        <thead>
          <tr>
            <th>Name</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <tr v-if="loading"><td colspan="2">{% trans 'Loading...' %}</td></tr>
          <tr v-if="!loading">
            <td><input class="form-control" type="text" required v-model="new_exercise.name" placeholder="{% trans 'Name' %}" /></td>
            <td>
              <button class="btn btn-link" v-on:click="createExercise(new_exercise)" type="submit"><i class="fas fa-plus"></i></button>
              <div v-if="pending_adding" class="spinner-border" role="status">
                <span class="sr-only">{% trans 'Loading...' %}</span>
              </div>
            </td>
          </tr>
          <tr v-if="!loading" v-for="exercise in exercises">
            <form>
            <td>
              <input class="form-control" type="text" v-model="exercise.name" />
            </td>
            <td>
              <button type="submit" class="btn btn-link" v-on:click="updateExercise(exercise)"><i class="fas fa-save"></i></button>
              <div v-if="pending_update == exercise.id" class="spinner-border" role="status"></div>

              <button type="button" class="btn btn-link" data-toggle="modal" v-bind:data-target="'#modal-delete-' + exercise.id"><i class="fas fa-trash"></i></button>
            </td>
            </form>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <div v-for="exercise in exercises" class="modal" tabindex="-1" role="dialog" v-bind:id="'modal-delete-' + exercise.id">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">{% trans 'Delete exercise' %}</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <p>{% trans 'Are you sure you want to delete this exercise?' %}</p>
          <p>[[ exercise.name ]]</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">{% trans 'Close' %}</button>
          <button type="button" class="btn btn-danger" v-on:click="deleteExercise(exercise)">{% trans 'Delete' %}</button>
        </div>
      </div>
    </div>
  </div>

</div>
{% endblock %}

{% block extrabody %}
<script src="{% static 'training/js/exercises.js' %}"></script>
{% endblock %}